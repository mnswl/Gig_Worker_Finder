# ----------------------
# Gig Worker Finder - Environment Variables Example
# Copy this file to `.env` and fill in real values.
# ----------------------

# Server
PORT=5000
MONGO_URI=mongodb://localhost:27017/gwf_db
JWT_SECRET=super_secret_string_change_me

# SMTP (Email Verification)
# Below settings assume Gmail SMTP; adjust for your provider.
# Gmail: create an App Password at https://myaccount.google.com/apppasswords and use it instead of your regular password.
# Mailtrap: https://mailtrap.io/ (free testing inbox) – replace host/port/user/pass accordingly.
# Ethereal: run `npx nodemailer create-test-account` to get throw-away creds.

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465           # 465 for SSL, use 587 with EMAIL_SECURE=false for STARTTLS
EMAIL_SECURE=true         # "true" for SSL, "false" for STARTTLS
EMAIL_USER=example@gmail.com
EMAIL_PASS=your_app_password_here
EMAIL_FROM="Gig Worker Finder <noreply@example.com>"
