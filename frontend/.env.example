# Example environment configuration for the frontend.
# Copy this file to `.env` or `.env.local` and adjust as needed.

# Comma-separated list of FX rate provider endpoints.
# Each URL must include the placeholder `{BASE}` which will be replaced
# by the ISO currency code (e.g. USD, EUR) at runtime.
REACT_APP_FX_PROVIDERS=https://v6.exchangerate-api.com/v6/<key>/latest/{BASE},https://api.exchangerate.host/latest?base={BASE}&access_key=<key>

# Google OAuth Client ID obtained from Google Cloud Console
REACT_APP_GOOGLE_CLIENT_ID=your_google_client_id_here
